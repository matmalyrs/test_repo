version: 2
jobs:
  plan:
    docker:
      - image: docker.io/mataly/toolbox-rs-pipeline-python:latest
    steps:
      - checkout
      - run: python3 /rackspace/authenticate.py

workflows:
  version: 2
  build_and_test:
    jobs:
      - plan